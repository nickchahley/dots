# many fun vanity tools I want to install require rust > 1.70, from apt is
# 1.66(?), so will try installing from rustup
# to uninstall: rustup self uninstall
# update: rustup update
curl https://sh.rustup.rs -sSf | sh

cargo install eza
cargo install du-dust
